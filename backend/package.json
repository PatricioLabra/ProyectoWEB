{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend del proyecto web",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "serve": "npm run build && node dist/index.js",
    "dev:serve": "nodemon --watch './**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PatricioLabra/ProyectoWEB.git"
  },
  "author": "Dan Rivaille & Patricio Labra",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PatricioLabra/ProyectoWEB/issues"
  },
  "homepage": "https://github.com/PatricioLabra/ProyectoWEB#readme",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0"
  }
}
